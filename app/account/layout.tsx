import './account.css';
export const metadata = { title: 'ESX — My account', description: 'Your ESX account, permanent QR code and private conversations.' };
export default function AccountLayout({ children }: { children: React.ReactNode }) { return children; }
